"""Settings for {{ project_name }}.

Extend :class:`pete_mcp_core.settings.BaseCoreSettings` with your own domain
fields. The base already covers transport, host, port, log_level, log_format,
auth_token, auth_required.
"""

from __future__ import annotations

from pete_mcp_core.settings import BaseCoreSettings


class Settings(BaseCoreSettings):
    """Add domain fields here.

    Example::

        weather_api_key: SecretStr = Field(
            default=SecretStr(""),
            validation_alias=AliasChoices(
                "WEATHER_API_KEY", "MCP_WEATHER_API_KEY"
            ),
        )
    """
